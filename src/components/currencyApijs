import React from 'react'
import axios from 'axios'

export default function currencyApi(a) {
  const apiKey = process.env.CURRENCY_API_KEY
  axios.get(

  )
  return (
    <div>currency-api</div>
  )
}
